This was a project to teach myself Rust. It implements multi-process loops set at various speeds each on a three phase switch, checking for incoming UDP messages. Uses tokio, socket2, std thread parallelism and sockets. 

